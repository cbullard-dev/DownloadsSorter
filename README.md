# Download Sorter

This is a simple application to sort the downloads director on a Mac or possibly a unix based system.

This project should be able to create directories if they don't already exist and sort items of specific types into relevant directories. This will also try and sort out items designed for windows systems like `*.exe` and `*.msi` packages.